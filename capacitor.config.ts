import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.myusagetracker',
  appName: 'my-usage-track-ledger',
  webDir: 'build'
};

export default config;
